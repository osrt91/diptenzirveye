import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "DZ Coin Mağazası | DiptenZirveye",
};

export default function CoinShopLayout({ children }: { children: React.ReactNode }) {
  return children;
}
